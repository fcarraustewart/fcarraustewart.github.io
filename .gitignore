# Bundler cache
.bundle
vendor
# Gemfile.lock is intentionally committed so CI pins the exact gem set
# (prevents silent theme-version drift breaking the build). See CLAUDE.md.

# Jekyll cache
.jekyll-cache
.jekyll-metadata
_site

# RubyGems
*.gem

# NPM dependencies
node_modules
package-lock.json

# IDE configurations
.idea
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/tasks.json

# Misc
_sass/dist
assets/js/dist
*.DS_Store
